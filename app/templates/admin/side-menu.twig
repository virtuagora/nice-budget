<aside class="menu">
	<ul class="menu-list">
		<li>
			<a href="{{path_for('showAdminOverview')}}">Inicio</a>
		</li>
	</ul>
 {% if not is_state('pre-upload-proposals') %}

	<p class="menu-label">
		Proyectos
	</p>
	<ul class="menu-list">
		{% if is_state('upload-proposals') %}
			<li>
				<a href="{{path_for('showAdminCreateProject')}}">
					<i class="fas fa-plus has-text-link"></i>&nbsp;&nbsp;Nuevo</a>
			</li>
		{% endif %}
      <li>
        <a href="{{path_for('showAdminProjects')}}">Listado</a>
      </li>
      <li>
        <a href="{{path_for('showAdminProjectsFeasible')}}">Factibles</a>
      </li>
      <li>
        <a href="{{path_for('showAdminProjectsNotFeasible')}}">No Factibles</a>
      </li>
	</ul>
	{% endif %}
	<p class="menu-label">
		Padrón
	</p>
	<ul class="menu-list">
		<li>
			<a href="{{path_for('showAdminCitizens')}}">Consulta</a>
		</li>
		{% if not is_state('pre-results') or not is_state('results') %}
			<li>
				<a href="{{path_for('showAddCitizen')}}">Nuevo registro</a>
			</li>
		{% endif %}
	</ul>
	{% if is_state('vote') %}
		<ul class="menu-list">
			<li>
				<a href="{{path_for('showRegistrosPendientes')}}">Registros pendientes</a>
			</li>
			<li>
				<a href="{{path_for('showVotersPendientes')}}">Votantes pendientes</a>
			</li>
		</ul>
	{% endif %}
	{% if is_state('vote') or is_state('pre-results') or is_state('results') %}
		<p class="menu-label">
			Blockchain
		</p>
		<ul class="menu-list">
			<li>
				<a href="{{path_for('showAdminSellado')}}">Sellados</a>
			</li>
		</ul>
	{% endif %}
	{% if is_state('pre-results') or is_state('results') %}
		<p class="menu-label">
			Escrutinio
		</p>
		<ul class="menu-list">
			<li>
				<a href="{{path_for('showBoletas')}}">Boletas en papel</a>
			</li>
			<li>
				<a href="{{path_for('showSeleccionadosAdmin')}}">Seleccionados</a>
			</li>
			<li>
				<a href="{{path_for('showEscrutinio')}}">Realizar escrutinio</a>
			</li>
		</ul>
	{% endif %}
	<p class="menu-label">
		Plataforma
	</p>
	<ul class="menu-list">
		<li>
			<a href="{{path_for('showOptions')}}">Opciones</a>
		</li>
		<li>
			<a href="{{path_for('showPageblocks')}}">Bloques de texto</a>
		</li>
		<li>
			<a href="{{path_for('showCustomization')}}">Personalización</a>
		</li>
		<li>
			<a href="{{path_for('showRoles')}}">Asignar roles</a>
		</li>
    <li>
      <a href="{{path_for('showAdminStats')}}">Estadísticas</a>
    </li>
	</ul>
</aside>
