{% extends 'admin/master.twig' %}

{% block section %}
  <h1 class="title is-1">Nuevo registro en el padron
  </h1>
<h2 class="subtitle is-4">
    Aqui puede agregar un nuevo registro en el padron.
</h2>
<hr>
  <add-citizen url="{{path_for('runAddCitizen')}}" :allow-restart="true" :allow-start-voting="false" url-participacion="{{path_for('runParticipacionPadron')}}">
  {% include 'base/loading.twig' %}
  </add-citizen>
{% endblock %}